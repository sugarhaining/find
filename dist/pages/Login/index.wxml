<block wx:if="{{$taroCompReady}}">
    <view class="{{anonymousState__temp}}">
        <image src="{{LoginPng}}"></image>
        <button class="{{anonymousState__temp2}}" open-type="getUserInfo" bindgetuserinfo="anonymousFunc0">授权登录</button>
    </view>
</block>